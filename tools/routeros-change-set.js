'use strict';
const crypto=require('crypto');
const {canonicalValue}=require('./routeros-semantic');
const RISK={low:1,review:2,high:3,critical:4};
const TYPES=new Set(['add','remove','change']);
const secret=/(?:password|passwd|secret|private[-_]?key|passphrase|token|credential)/i;
function sha256(v){return crypto.createHash('sha256').update(v,'utf8').digest('hex')}
function redact(v){if(Array.isArray(v))return v.map(redact);if(v&&typeof v==='object')return Object.keys(v).sort().reduce((o,k)=>{o[k]=secret.test(k)?'[REDACTED]':redact(v[k]);return o},{});return v}
function assertDiff(d){if(!d||typeof d!=='object'||!d.summary||!Array.isArray(d.changes)||!Array.isArray(d.conflicts))throw new TypeError('Invalid semantic diff contract')}
function classifyRisk(c){if(c.risk==='critical'||c.risk==='high')return c.risk;if(c.type==='remove')return'high';if(c.requires_review)return'review';return'low'}
function createChangeSet(diff,options={}){assertDiff(diff);const conflicts=diff.conflicts.map(redact);const changes=diff.changes.map(c=>{if(!TYPES.has(c.type))throw new Error('Unsupported change type: '+c.type);return redact({id:c.id,type:c.type,kind:c.kind,identity:c.identity,path:c.path||null,risk:classifyRisk(c),requires_review:c.requires_review!==false,attribute_changes:c.attribute_changes||[],order_change:c.order_change||null,before:c.before||null,after:c.after||null})});changes.sort((a,b)=>(RISK[b.risk]-RISK[a.risk])||a.id.localeCompare(b.id));const risk=changes.reduce((h,c)=>RISK[c.risk]>RISK[h]?c.risk:h,'low');const blocked=conflicts.length>0||risk==='critical';const set={schema_version:'1.0',change_set:{name:'RouterOS Structured Change Set',version:'1.0',id:options.id||'changeset:'+sha256(JSON.stringify(canonicalValue({diff:diff.fingerprint,changes}))).slice(0,20),state:blocked?'blocked':'draft',read_only:true,requires_approval:changes.some(c=>c.requires_review)},target:{routeros:diff.routeros||null,actual_fingerprint:diff.actual?.fingerprint||null,desired_fingerprint:options.desired_fingerprint||null},snapshot:{required:true,captured:false,reference:null},validation:{diff_fingerprint:diff.fingerprint||null,conflicts:conflicts.length,valid:!blocked},risk:{level:risk,changes:changes.length},approval:{status:'pending',approved_by:null,approved_at:null},execution:{status:'not_started',deployment_id:null},verification:{required:true,status:'not_started',checks:[]},rollback:{required:changes.length>0,prepared:false,artifact:null,status:'not_started'},conflicts,changes};set.fingerprint=sha256(JSON.stringify(canonicalValue(set)));return set}
function prepareRollback(cs){if(!cs||!Array.isArray(cs.changes))throw new TypeError('Change Set is required');const commands=cs.changes.map(c=>c.type==='add'&&c.after?.attributes?.['.id']?{action:'remove',path:c.path,id:c.after.attributes['.id']}:c.type==='add'?{action:'manual',reason:'Added resource has no stable .id; use snapshot restore or explicit reverse command.',change_id:c.id}:{action:'restore-resource',change_id:c.id,path:c.path,resource:c.before||null});const artifact={schema_version:'1.0',change_set_id:cs.change_set.id,generated_at:null,reversible:commands.every(c=>c.action!=='manual'),commands:redact(commands)};return {...cs,rollback:{...cs.rollback,prepared:true,artifact,status:'prepared'}}}
module.exports={createChangeSet,prepareRollback,redact,classifyRisk};