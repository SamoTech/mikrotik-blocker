# Vite automatically proxies /api to the backend in dev mode (see vite.config.js)
# No extra env vars needed for development.
# For production builds, set the API base URL here:
VITE_API_BASE_URL=http://localhost:3001
